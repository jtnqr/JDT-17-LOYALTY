# Database
DB_NAME=jdt17_loyalty
DB_USER=loyalty
DB_PASSWORD=change_me_in_production
DB_PORT=5432

# JWT (generate: openssl rand -hex 64)
JWT_SECRET=change-this-to-a-real-secret-minimum-256-bits-use-openssl-rand-hex-64

# Frontend → Backend
API_URL=http://localhost:8080
NEXT_PUBLIC_REFETCH_INTERVAL=5000
